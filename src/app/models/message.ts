export class message {
    message: string;
    currentDate: Date;
    
    constructor(message: string, currentDate: Date) {
        this.message = message;
        this.currentDate = currentDate;
    }
}